#!/usr/bin/env bash
set -euo pipefail

args=()
for arg in "$@"; do
  case "$arg" in
    --no-out-link)
      args+=(--no-link)
      ;;
    *)
      args+=("$arg")
      ;;
  esac
done

exec nix --extra-experimental-features nix-command build "${args[@]}"
